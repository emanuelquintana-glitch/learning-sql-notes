---
title: "Learning SQL — Beaulieu (2020)"
date: "2026-04-19"
format:
  html:
    toc: true
    toc-depth: 3
  pdf:
    toc: true
---

```{=org}
#+CATEGORY: Lenguajes de Programacion
```
```{=org}
#+FILETAGS: :libro:lenguajes_de_programacion:
```
```{=org}
#+ROAM_ALIASES: "Learning SQL_ Generate, Manipulate, and Retrieve Data"
```
# Metadatos {#metadatos tipo="libro"}

  Campo       Valor
  ----------- -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  Título      Learning SQL\_ Generate, Manipulate, and Retrieve Data
  Autor       Alan Beaulieu
  Año         2020
  Editorial   O\'Reilly Media, Incorporated
  Categoría   Lenguajes de Programacion
  Iniciado    2026-04-18
  Archivo     [PDF](file:///home/emanuel/Biblioteca/cursos_mit/Electrical Engineering and Computer Science (Course 6)/02_Programming_Languages/Learning SQL_ Generate, Manipulate, and Retrieve Data -- Alan Beaulieu -- 2020 -- O'Reilly Media, Incorporated -- 9781492057611 -- 089bb3c2d0f555be4c292251f45c4863 -- Anna’s Archive.pdf)

# Resumen ejecutivo

# Por qué lo leo / Objetivo de lectura

# Ideas principales

## Idea 1

## Idea 2

## Idea 3

# Notas por capítulo

## Capítulo 1

### Resumen

### Conceptos clave

### Preguntas y dudas

# Citas y fragmentos destacados

# Vocabulario técnico nuevo

  Término   Definición   Página
  --------- ------------ --------
                         

# Ejercicios y problemas resueltos

# Código relevante

``` {.python results="output"}
# Ejemplo del libro
```

# Conexiones con otros libros/conceptos

# Aplicaciones prácticas

# Valoración final

Dificultad
:   \[ \] Fácil \[ \] Medio \[ \] Difícil \[ \] Muy difícil

Utilidad
:   \[ \] Baja \[ \] Media \[ \] Alta \[ \] Esencial

Recomendaría
:   \[ \] Sí \[ \] No \[ \] Con reservas

Releer
:   \[ \] Sí \[ \] No

## Opinión personal

# Anotaciones del PDF

# Notas por capítulo

## Capítulo 1 --- A Little Background

### Resumen

SQL es un lenguaje diseñado para interactuar con bases de datos relacionales. El modelo relacional (Codd, 1970) organiza los datos en tablas (relaciones) con filas (tuplas) y columnas (atributos). SQL tiene tres sublengajes:

- **DDL** (Data Definition Language): CREATE, ALTER, DROP
- **DML** (Data Manipulation Language): SELECT, INSERT, UPDATE, DELETE
- **DCL** (Data Control Language): GRANT, REVOKE

### Conceptos clave

- **Entidad**: objeto del mundo real representado en la BD (cliente, cuenta)
- **Clave primaria**: columna(s) que identifican únicamente cada fila
- **Clave foránea**: columna que referencia la PK de otra tabla
- **Normalización**: proceso de eliminar redundancia en las tablas
- **Conjunto de resultados**: tabla no persistente devuelta por un SELECT

### Base de datos del libro

La base de datos bancaria (`bank`) tiene estas tablas principales:

  Tabla              Descripción
  ------------------ ----------------------------------
  department         Departamentos del banco
  employee           Empleados
  branch             Sucursales
  product            Tipos de cuenta/préstamo
  customer           Clientes (individuos y empresas)
  individual         Datos personales de clientes
  business           Datos de empresas clientes
  account            Cuentas bancarias
  acc~transaction~   Transacciones

### Setup --- crear la base de datos

``` {.duckdb db="~/repos/github/learning-sql-notes/databases/practice/bank.duckdb" results="output"}
-- Crear todas las tablas con datos
-- (pegar contenido de src/sql/beaulieu/01/bank_schema.sql)
SELECT 'Base de datos lista' AS status;
```

### Primeros queries --- explorar las tablas

``` {.duckdb db="~/repos/github/learning-sql-notes/databases/practice/bank.duckdb"}
-- ¿Cuántos empleados hay?
SELECT COUNT(*) AS total_empleados FROM employee;
```

``` {.duckdb db="~/repos/github/learning-sql-notes/databases/practice/bank.duckdb"}
-- Ver todos los empleados con su departamento
SELECT e.emp_id,
       e.fname || ' ' || e.lname AS nombre,
       e.title,
       d.name AS departamento
FROM employee e
JOIN department d ON e.dept_id = d.dept_id
ORDER BY e.emp_id;
```

``` {.duckdb db="~/repos/github/learning-sql-notes/databases/practice/bank.duckdb"}
-- Cuentas activas con saldo disponible
SELECT a.account_id,
       i.fname || ' ' || i.lname AS cliente,
       a.product_cd AS tipo,
       a.avail_balance AS saldo
FROM account a
JOIN customer c ON a.cust_id = c.cust_id
JOIN individual i ON c.cust_id = i.cust_id
WHERE a.status = 'ACTIVE'
ORDER BY a.avail_balance DESC;
```

``` {.duckdb db="~/repos/github/learning-sql-notes/databases/practice/bank.duckdb"}
-- Resumen de saldos por tipo de producto
SELECT product_cd,
       COUNT(*)              AS num_cuentas,
       SUM(avail_balance)    AS saldo_total,
       AVG(avail_balance)    AS saldo_promedio
FROM account
WHERE status = 'ACTIVE'
GROUP BY product_cd
ORDER BY saldo_total DESC;
```

### Preguntas y dudas

- ¿Por qué `individual` y `business` son tablas separadas de `customer`? → Herencia de tabla: `customer` tiene los campos comunes, las subclases tienen los específicos. Patrón común en modelado relacional.

- ¿Qué es `pending_balance` vs `avail_balance`? → `avail_balance`: saldo disponible ahora. → `pending_balance`: incluye transacciones no liquidadas aún.
